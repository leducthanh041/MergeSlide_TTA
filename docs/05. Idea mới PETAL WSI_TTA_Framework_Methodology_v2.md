# Framework TTA cho WSI: Tích hợp PETAL + DaPC + NOTE + CascadeTTA + WSI Feature Augmentation

> **Phiên bản:** 2.0 — Cập nhật hàm loss thực tế và hướng dẫn chạy ablation
> **Bối cảnh:** Test-Time Adaptation (TTA) cho mô hình MergeSlide (TITAN-based) trên các tác vụ phân loại ung thư WSI đa site, gồm cả setting IND và OOD.

---

## 1. Bài toán và Động lực

### 1.1 Thiết lập bài toán

Cho mô hình `merge_final` — mô hình MergeSlide đã được huấn luyện tuần tự trên $T$ tasks, với tham số hợp nhất $\tilde{\theta}_{1:T}$. Tại inference, với mỗi task $t$ đến, mô hình phải thích ứng với một luồng WSI **không có nhãn** $\mathcal{U}^t = \{x_m^t\}_{m=1}^{M_t}$ trong điều kiện:

- **IND setting:** Tập test có site overlap cao với tập train (BRCA: 20.5, RCC: 30.3, NSCLC: 37.1). Tuy nhiên vẫn tồn tại một tỷ lệ nhỏ OOD samples.
- **OOD setting:** 100% mẫu test đến từ các site hoàn toàn mới (site overlap = 0). OOD validation rất nhỏ và bất ổn định (ESCA val min=1, TGCT val min=1).
- **Ràng buộc cứng:** Không có nhãn, không lưu dữ liệu cũ, không reset mô hình về scratch.

### 1.2 Kiến trúc MergeSlide — điểm khác biệt quan trọng so với natural image TTA

Mô hình MergeSlide thực tế gồm hai thành phần:

```
backbone = titan.vision_encoder     ← CẬP NHẬT tại TTA (LayerNorm params)
mlp      = Linear(768, n_classes)   ← FROZEN, init từ prompt prototypes
```

Forward pass cho một slide:

$$Z_i = \text{backbone}(\text{features},\, \text{coords},\, \text{ps}) \in \mathbb{R}^{768}$$
$$\text{logits} = \text{mlp}(Z_i) = Z_i \cdot W_{class}^\top + b \in \mathbb{R}^{n_{classes}}$$

trong đó $W_{class} \in \mathbb{R}^{n_{classes} \times 768}$ là **prompt prototype weights** — cột $j$ là embedding của class $j$ từ TITAN text encoder, frozen hoàn toàn. Điều này có nghĩa là $\text{logits}_j = Z_i \cdot e_j^\top$ — đúng là dot-product similarity với class embeddings, nhưng được implement qua `nn.Linear` frozen.

**Hệ quả cho TTA:** Gradient từ loss chỉ chảy qua `backbone` (vì `mlp` frozen). Mục tiêu của tất cả loss components là buộc `backbone` tạo ra slide embeddings $Z_i$ ngày càng align tốt hơn với frozen class prototypes trong điều kiện domain shift.

### 1.3 Các thách thức cần giải quyết đồng thời

| Thách thức | Nguồn gốc |
|---|---|
| Catastrophic forgetting | Cập nhật backbone làm mất tri thức từ $T$ tasks đã merge |
| Tích tụ lỗi (error accumulation) | Pseudo-labels nhiễu tích lũy theo thời gian |
| IND/OOD mixed stream | IND: có % nhỏ OOD; OOD: 100% mẫu OOD |
| Class imbalance | CESC, BRCA mất cân bằng mạnh; RCC lệch class 2 |
| OOD val cực nhỏ và bất ổn | ESCA val min=1, TGCT val min=1 → không thể tuning trên OOD val |
| Nhãn giả không tin cậy | Đặc biệt khi domain shift lớn (setting OOD hoàn toàn) |

---

## 2. Kiến trúc Tổng thể

Framework **MergeSlide-TTA** tích hợp 5 module theo sơ đồ:

```
WSI (features [N,768] + coords [N,2])
        │
        ▼
[Module A] WSI Bag-level Augmentation
   K lần subsample → K bag variations {B^(k)}
        │
        ├──────────────────────┐
        ▼                      ▼
   Student backbone        Teacher backbone (EMA)
   (cập nhật backprop)     (θ' = π·θ' + (1-π)·θ_s)
        │                      │
        │   [Module B] DaPC Pseudo-label Correction
        │   anchor confidence vs teacher → y_corrected
        │                      │
        └──────────┬────────────┘
                   ▼
   [Module C] Hàm Loss Tổng hợp
   L_total = L_PETAL + γ_class·L_class + γ_task·L_task
                   │
                   ▼
   [Module D] FIM-based Parameter Restoration
   Reset params có F_p < quantile(F,δ) về θ_0
                   │
                   ▼
   [Module E] Adaptive Learning Rate (OOD-score based)
   η_eff = η_base · σ(OOD_score − τ_OOD)
```

---

## 3. Chi tiết từng Module

### Module A: WSI Bag-level Augmentation

#### Vấn đề
PETAL gốc dùng $\tilde{y}' = \frac{1}{K}\sum_{i=1}^K p(y|\alpha_i(\bar{x}), \theta')$ với $\alpha_i$ là pixel augmentation (xoay, flip, crop). Với WSI, input tại TTA time là bag embeddings đã được pre-extracted và frozen — pixel augmentation không áp dụng được.

#### Giải pháp: WSI Feature Augmentation (từ Frozen-to-Paraffin)

Augment ở **cấp phân phối đặc trưng túi** bằng patch subsampling ngẫu nhiên:

$$B_i^{(k)} = \text{RandomSample}(B_i,\ r_{patch} \cdot |B_i|), \quad k = 1, \ldots, K$$

với $r_{patch} \in \{0.50, 0.75\}$. Augmentation-averaged teacher prediction:

$$\tilde{y}' = \frac{1}{K} \sum_{k=1}^{K} \text{softmax}\!\left(\text{mlp}\!\left(\text{backbone}(B_i^{(k)}, \theta')\right)\right)$$

**Lý do hiệu quả:** Phân bố bệnh học không đồng đều trên WSI (patchy distribution) — mỗi subsample nhấn mạnh vùng mô khác nhau, tạo ra $K$ góc nhìn semantic độc lập về cùng slide.

**Lợi thế tính toán:** Chỉ tốn $K$ lần forward qua backbone (TITAN transformer), không cần recompute CNN patch features.

---

### Module B: DaPC Pseudo-label Correction (từ WeiContrast-DaPC)

DaPC so sánh confidence của **anchor model** $\theta_0$ (frozen source) và teacher để phát hiện domain shift và hiệu chỉnh nhãn giả.

**Bước 1 — Chọn pseudo-label dựa trên anchor confidence:**

$$y^{teacher} = \begin{cases}
\hat{y}' = \text{softmax}(\text{mlp}(\text{backbone}(B_i, \theta'))), & \text{nếu } \max\hat{y}_{anchor} \geq \tau_{ap} \quad \text{[IND]} \\
\tilde{y}' = \frac{1}{K}\sum_k \text{softmax}(\text{mlp}(\text{backbone}(B_i^{(k)}, \theta'))), & \text{otherwise} \quad \text{[OOD]}
\end{cases}$$

**Bước 2 — Blend với anchor prediction:**

$$y^{corrected} = \begin{cases}
\tilde{y}' & \text{nếu } \beta \cdot \max\tilde{y}' > \max\hat{y}_{anchor} \quad \text{[OOD nặng]} \\
\frac{1}{2}(\tilde{y}' + \hat{y}_{anchor}) & \text{otherwise} \quad \text{[IND hoặc OOD nhẹ]}
\end{cases}$$

trong đó $\hat{y}_{anchor} = \text{softmax}(\text{mlp}(\text{backbone}(B_i, \theta_0)))$ và $\beta \geq 1$.

**Hyperparameters:** $\tau_{ap} = 0.92$ (IND), $0.70$ (OOD); $\beta = 1.2$ (IND), $1.5$ (OOD).

---

### Module C: Hàm Loss Tổng hợp

#### Công thức tổng quát

$$\boxed{\mathcal{L}_{total} = \mathcal{L}_{PETAL} + \gamma_{class} \cdot \mathcal{L}_{class} + \gamma_{task} \cdot \mathcal{L}_{task}}$$

#### Thành phần 1: $\mathcal{L}_{PETAL}$ — PETAL Bayesian Loss

$$\mathcal{L}_{PETAL} = \underbrace{-\text{spw} \cdot \log q(\theta_s)}_{\text{Regularizer: giữ backbone gần source}} + \underbrace{\bar{\lambda} \cdot H^{xe}(y^{corrected}, y_{student})}_{\text{Cross-entropy: thích ứng target}}$$

**Regularizer Bayesian** $\log q(\theta_s)$: Gaussian posterior xấp xỉ bằng SWAG-diagonal

$$\log q(\theta_s) = -\frac{1}{2} \sum_p \frac{(\theta_{s,p} - \mu_p)^2}{\sigma^2_p + \epsilon}$$

với $\mu_p, \sigma^2_p$ từ `train_swag.py`. Tham số có $\sigma^2_p$ nhỏ (source chắc chắn) bị phạt nặng hơn khi thay đổi — đây là EWC tự nhiên từ Bayesian formulation.

**Cross-entropy** $H^{xe}$: student prediction phải khớp với pseudo-label đã hiệu chỉnh

$$H^{xe}(y^{corrected}, y_{student}) = -\sum_j y^{corrected}_j \cdot \log \text{softmax}(\text{logits}_{student})_j$$

**Ý nghĩa:** $\mathcal{L}_{PETAL}$ là CoTTA + Bayesian regularizer. Khi `spw=0`, degrades thành CoTTA.

#### Thành phần 2: $\mathcal{L}_{class}$ — Class Alignment với Temperature Scaling

$$\mathcal{L}_{class} = -\sum_j y^{corrected}_j \cdot \log \text{softmax}\!\left(\frac{\text{logits}_{student}}{\tau_c}\right)_j$$

Đây là soft cross-entropy **với temperature** $\tau_c = 0.07$ trên cùng logits của student. Gradient buộc backbone tạo $Z_i$ align với frozen class prototype weights.

**Tại sao cần thêm $\mathcal{L}_{class}$ khi đã có $\mathcal{L}_{PETAL}$?**

$\mathcal{L}_{PETAL}$ là consistency loss (student đồng ý với teacher về phân phối). $\mathcal{L}_{class}$ là alignment loss (slide embedding align với class prototypes). Khi $\tau_c = 1$, hai thành phần trùng công thức — nhưng $\tau_c$ nhỏ làm logits sắc nét hơn, tạo gradient mạnh hơn cho backbone để push $Z_i$ xa khỏi decision boundary, đặc biệt quan trọng với imbalanced classes (CESC, BRCA).

#### Thành phần 3: $\mathcal{L}_{task}$ — TCP Task Alignment

$$\mathcal{L}_{task} = \text{CE}\!\left(Z_s \cdot \mathcal{E}^T{}^\top,\; \hat{t}\right)$$

trong đó $Z_s = \text{backbone}(\cdot, \theta_s)$, $\mathcal{E}^T \in \mathbb{R}^{T \times 768}$ là frozen task-level prompt embeddings từ `task_prompts.pt`, và $\hat{t}$ là task ID được TCP xác định từ **anchor model** (stable reference, không bị drift).

Loss này giữ backbone không làm mất khả năng phân biệt task qua TCP inference trong quá trình adaptation.

#### Bảng tóm tắt gradient signal

| Loss | Buộc backbone làm gì | Giải quyết vấn đề gì |
|---|---|---|
| $-\text{spw} \cdot \log q(\theta_s)$ | Không drift xa $\mu_{SWAG}$ | Catastrophic forgetting |
| $\bar{\lambda} \cdot H^{xe}(y^{corrected}, y_{student})$ | Student đồng ý với teacher | Thích ứng target domain |
| $\gamma_{class} \cdot \mathcal{L}_{class}$ | $Z_i$ align với class prototypes | Class-level alignment, imbalance |
| $\gamma_{task} \cdot \mathcal{L}_{task}$ | $Z_i$ phân biệt được task | TCP duy trì task routing |

---

### Module D: FIM-based Parameter Restoration (từ PETAL)

Sau optimizer step, tính FIM để xác định tham số nào quan trọng với dữ liệu hiện tại:

$$F = \text{Diag}\!\left(\nabla_\theta \mathcal{L}_{total}\right)\!\left(\nabla_\theta \mathcal{L}_{total}\right)^\top$$

Reset các tham số **không quan trọng** (FIM thấp) về source model $\theta_0$:

$$\gamma = \text{quantile}(F, \delta), \quad m_p = \mathbb{1}[F_p < \gamma]$$

$$\theta_{t+1} = \mathbf{m} \odot \theta_0 + (\mathbf{1} - \mathbf{m}) \odot \theta_{t+1}$$

$\delta = 0.03$ nghĩa là 3% tham số có FIM thấp nhất (ít quan trọng nhất) bị reset về source sau mỗi step.

---

### Module E: Adaptive Learning Rate (từ NOTE-IABN)

**OOD score** cho mỗi slide:

$$\text{OOD\_score}(x) = 1 - \frac{\max \hat{y}_{anchor}}{\max \hat{y}_{teacher} + \epsilon}$$

Giá trị cao → anchor mất confidence → sample là OOD rõ ràng.

**Adaptive learning rate:**

$$\eta_{eff} = \eta_{base} \cdot \sigma\!\left(\text{OOD\_score} - \tau_{OOD}\right), \quad \tau_{OOD} = 0.5$$

IND samples: $\eta_{eff}$ thấp → adaptation nhẹ → bảo tồn source knowledge. OOD samples: $\eta_{eff}$ cao → adaptation mạnh hơn.

---

## 4. Thuật toán Tổng hợp

```
Algorithm: MergeSlide-TTA (PETAL-WSI)

Input:
  - merge_final model: backbone với θ_s = θ_0 = θ̃_{1:T}
  - Teacher: θ' = θ_0 (khởi tạo, EMA của student)
  - Anchor: θ_0 (frozen hoàn toàn, không update)
  - mlp: Linear(768, n_classes) — frozen, init từ prompt prototypes
  - Posterior: μ_SWAG, σ²_SWAG từ train_swag.py
  - task_prompts: E^T ∈ R^{T×768} — frozen

Hyperparameters (IND | OOD):
  - K=8,  r_patch=0.75 | 0.50   (bag augmentation)
  - τ_ap=0.92 | 0.70            (anchor confidence threshold)
  - β=1.2 | 1.5                 (DaPC sensitivity)
  - spw=1e-6,  lam_ce=1.0       (PETAL weights)
  - γ_class=1.0, γ_task=0.5     (additional loss weights)
  - τ_c=0.07                    (L_class temperature)
  - δ=0.03                      (FIM quantile)
  - π=0.999                     (EMA alpha)
  - η_base=1e-4, τ_OOD=0.5      (adaptive lr)

For each slide (features, coords, task_id):

  // === PHASE 1: Pseudo-label Generation ===
  1. idx = randperm(N)[:K_PATCHES]
     feat_std, cord_std = features[idx], coords[idx]

  2. ŷ_anchor = softmax(mlp(backbone(feat_std, cord_std, θ_0)))
     ŷ_teacher = softmax(mlp(backbone(feat_std, cord_std, θ')))

  3. OOD_score = 1 − max(ŷ_anchor) / (max(ŷ_teacher) + ε)
     η_eff = η_base · σ(OOD_score − τ_OOD)

  4. if max(ŷ_anchor) < τ_ap:  // OOD → aug-averaging
       ỹ' = (1/K) Σ_k softmax(mlp(backbone(B^(k), θ')))
     else:                       // IND → direct
       ỹ' = ŷ_teacher

  5. DaPC: if β·max(ỹ') > max(ŷ_anchor):
             y_corrected = ỹ'
           else:
             y_corrected = 0.5·(ỹ' + ŷ_anchor)

  // === PHASE 2: Student Forward ===
  6. Z_s = backbone(feat_std, cord_std, θ_s)
     logits_s = mlp(Z_s)
     y_student = softmax(logits_s)

  // === PHASE 3: Loss Computation ===
  7. log_q = −0.5·Σ_p (θ_{s,p}−μ_p)²/(σ²_p+ε)
     L_PETAL  = −spw·log_q + lam_ce·H_xe(y_corrected, y_student)
     L_class  = −Σ_j y_corrected_j·log_softmax(logits_s/τ_c)_j
     L_task   = CE(Z_s @ E^T.T, t̂)  // t̂ = TCP từ anchor
     L_total  = L_PETAL + γ_class·L_class + γ_task·L_task

  // === PHASE 4: Student Update ===
  8. Tính FIM: F = Diag(∇L_total)(∇L_total)^T
     lr_scale optimizer theo η_eff
     optimizer.step()

  // === PHASE 5: Teacher EMA ===
  9. θ' ← π·θ' + (1−π)·θ_s

  // === PHASE 6: FIM Restoration ===
  10. γ = quantile(F, δ)
      m_p = 1 if F_p < γ else 0
      θ_s ← m⊙θ_0 + (1−m)⊙θ_s

  // === PHASE 7: Inference ===
  11. Return argmax softmax(mlp(backbone(feat_std, cord_std, θ')))
```

---

## 5. Phân tích Lý thuyết

### 5.1 Chống Catastrophic Forgetting

Ba cơ chế hoạt động song song:

**a) Regularizer Bayesian:** $\log q(\theta_s)$ phạt drift khỏi posterior source. Tham số có $\sigma^2_p$ nhỏ (source chắc chắn) được bảo vệ mạnh hơn.

**b) FIM Restoration:** Reset tham số ít quan trọng về $\theta_0$ sau mỗi step. Chỉ giữ lại thay đổi của tham số thực sự cần cho dữ liệu hiện tại.

**c) Anchor model cố định:** $\theta_0$ không bao giờ cập nhật — đóng vai trò bộ nhớ dài hạn bất biến trong DaPC và L_task.

### 5.2 Chống Tích tụ Lỗi

**a) DaPC:** Kéo pseudo-labels về phía anchor khi domain shift nhẹ → giảm error propagation.

**b) Augmentation averaging:** Ensemble $K$ góc nhìn của cùng slide → pseudo-labels mịn hơn, ít bị ảnh hưởng bởi local noise.

**c) Adaptive learning rate:** $\eta_{eff}$ thấp cho IND samples → không over-adapt, giảm tốc độ tích lũy lỗi.

### 5.3 Xử lý IND/OOD Mixed Stream

| Tình huống | Hành vi |
|---|---|
| 100% IND | OOD_score thấp → η_eff nhỏ → thích ứng rất nhẹ; DaPC blend với anchor |
| Mixed (IND + OOD nhỏ) | OOD samples có η_eff cao hơn → thích ứng chọn lọc |
| 100% OOD | OOD_score cao → adaptation mạnh; DaPC dùng aug-averaged teacher |

---

## 6. Ablation Studies — Thiết kế và Cách Chạy

### 6.1 Chuẩn bị: Tạo các file config ablation

Tất cả ablation đều chạy qua script hiện có, chỉ đổi biến `CONFIG`. Mỗi file config chỉ cần khai báo các key khác với `default.yaml`.

---

### 6.2 Nhóm A: Ablation theo tham số được cập nhật

| ID | Tên | Tham số cập nhật | Config key |
|---|---|---|---|
| **A-1** | LN only (baseline) | LayerNorm (26k params) | `trainable_params: "ln_only"` |
| **A-3** | LN + QKV | LayerNorm + QKV attention (10.6M params) | `trainable_params: "ln_qkv"` |

**Chạy A-1 (baseline, mặc định):**
```bash
bash scripts/run_tta_ind.sh
bash scripts/run_tta_ood.sh
```

**Tạo config cho A-3:**
```yaml
# configs/default_abl_ln_qkv.yaml
defaults:
  - default
tta:
  trainable_params: "ln_qkv"
  lr: 5.0e-5
```

**Chạy A-3:**
```bash
CONFIG=configs/default_abl_ln_qkv.yaml bash scripts/run_tta_ind.sh
CONFIG=configs/default_abl_ln_qkv.yaml bash scripts/run_tta_ood.sh
```

---

### 6.3 Nhóm B: Ablation theo cơ chế chống forgetting/error

| ID | Tên | Mô tả | Cách implement |
|---|---|---|---|
| **B-1** | CoTTA equivalent | Tắt regularizer Bayesian | `spw: 0.0` |
| **B-2** | PETAL (no DaPC) | Tắt DaPC, chỉ dùng direct teacher | `dapc_beta: 999.0` |
| **B-3** | PETAL + DaPC (no bag aug) | Tắt bag augmentation | `k_aug: 1, r_patch: 1.0` |
| **B-4** | Full framework | Tất cả cơ chế bật | default config |

**Tạo configs:**

```yaml
# configs/default_abl_cotta.yaml
tta:
  spw: 0.0               # tắt log q(θ) → tương đương CoTTA
```

```yaml
# configs/default_abl_no_dapc.yaml
tta:
  dapc_beta: 999.0       # β rất cao → điều kiện DaPC luôn True → không blend anchor
```

```yaml
# configs/default_abl_no_aug.yaml
tta:
  k_aug:   1
  r_patch: 1.0           # không subsample, không augmentation
```

**Chạy:**
```bash
CONFIG=configs/default_abl_cotta.yaml   bash scripts/run_tta_ind.sh
CONFIG=configs/default_abl_no_dapc.yaml bash scripts/run_tta_ind.sh
CONFIG=configs/default_abl_no_aug.yaml  bash scripts/run_tta_ind.sh
```

---

### 6.4 Nhóm C: Ablation theo chiến lược augmentation WSI

| ID | Tên | $r_{patch}$ | $K$ | Ghi chú |
|---|---|---|---|---|
| **C-1** | No aug | 1.0 | 1 | Baseline không augment (= B-3 trên) |
| **C-2** | Aug1 (IND default) | 0.75 | 8 | Mặc định IND |
| **C-3** | Aug2 (OOD default) | 0.50 | 8 | Mặc định OOD |
| **C-4** | K=4 | 0.75 | 4 | Ít iteration hơn |

**Tạo configs:**
```yaml
# configs/default_abl_k4.yaml
tta:
  k_aug:   4
  r_patch: 0.75
```

```yaml
# configs/default_abl_aug2_ind.yaml   (thử Aug2 trên IND)
tta:
  k_aug:   8
  r_patch: 0.50
```

**Chạy:**
```bash
CONFIG=configs/default_abl_k4.yaml      bash scripts/run_tta_ind.sh
CONFIG=configs/default_abl_aug2_ind.yaml bash scripts/run_tta_ind.sh
```

---

### 6.5 Nhóm D: Ablation theo loss components

| ID | Tên | $\gamma_{class}$ | $\gamma_{task}$ | Ý nghĩa |
|---|---|---|---|---|
| **D-1** | No L_class | 0.0 | 0.5 | Chỉ L_PETAL + L_task |
| **D-2** | No L_task | 1.0 | 0.0 | Chỉ L_PETAL + L_class |
| **D-3** | L_PETAL only | 0.0 | 0.0 | Chỉ PETAL gốc |
| **D-4** | Full (default) | 1.0 | 0.5 | Tất cả loss components |

```yaml
# configs/default_abl_no_lclass.yaml
tta:
  gamma_class: 0.0

# configs/default_abl_no_ltask.yaml
tta:
  gamma_task: 0.0

# configs/default_abl_petal_only.yaml
tta:
  gamma_class: 0.0
  gamma_task:  0.0
```

**Chạy:**
```bash
CONFIG=configs/default_abl_no_lclass.yaml bash scripts/run_tta_ind.sh
CONFIG=configs/default_abl_no_ltask.yaml  bash scripts/run_tta_ind.sh
CONFIG=configs/default_abl_petal_only.yaml bash scripts/run_tta_ind.sh
```

---

### 6.6 Tổng hợp tất cả ablation cần chạy

```bash
# === NHÓM A: Tham số được cập nhật ===
bash scripts/run_tta_ind.sh                                              # A-1: LN only (baseline)
CONFIG=configs/default_abl_ln_qkv.yaml bash scripts/run_tta_ind.sh     # A-3: LN + QKV

# === NHÓM B: Cơ chế chống forgetting ===
CONFIG=configs/default_abl_cotta.yaml    bash scripts/run_tta_ind.sh   # B-1: CoTTA equivalent
CONFIG=configs/default_abl_no_dapc.yaml  bash scripts/run_tta_ind.sh   # B-2: no DaPC
CONFIG=configs/default_abl_no_aug.yaml   bash scripts/run_tta_ind.sh   # B-3: no bag aug

# === NHÓM C: Augmentation strategy ===
CONFIG=configs/default_abl_k4.yaml       bash scripts/run_tta_ind.sh   # C-4: K=4
CONFIG=configs/default_abl_aug2_ind.yaml  bash scripts/run_tta_ind.sh  # C-3: r_patch=0.50

# === NHÓM D: Loss components ===
CONFIG=configs/default_abl_no_lclass.yaml bash scripts/run_tta_ind.sh  # D-1: no L_class
CONFIG=configs/default_abl_no_ltask.yaml   bash scripts/run_tta_ind.sh # D-2: no L_task
CONFIG=configs/default_abl_petal_only.yaml bash scripts/run_tta_ind.sh # D-3: PETAL only

# === OOD setting (tương tự, đổi script) ===
bash scripts/run_tta_ood.sh
CONFIG=configs/default_abl_no_dapc.yaml  bash scripts/run_tta_ood.sh
# ... tương tự các nhóm trên với run_tta_ood.sh
```

> **Lưu ý thứ tự ưu tiên:** Nhóm D quan trọng nhất để validate đóng góp của từng loss component. Nhóm B để so sánh với CoTTA/PETAL gốc. Nhóm A và C chạy sau khi đã có kết quả baseline.

---

## 7. Metrics Đánh giá

### 7.1 Metrics chính

- **Balanced Accuracy (bACC):** Ưu tiên cho CESC, BRCA (imbalanced). $\text{bACC} = \frac{1}{C}\sum_c \text{Recall}_c$
- **ACC:** Overall accuracy sau adaptation

### 7.2 Metrics bổ sung

- **Per-class Recall và Precision:** Debug pseudo-label quality, đặc biệt cho imbalanced tasks
- **OOD score trung bình:** Kiểm tra Module E hoạt động đúng không
- **Loss breakdown:** `loss_petal`, `loss_class`, `loss_task` theo từng slide để debug tích tụ lỗi

### 7.3 Tránh ảnh hưởng bởi OOD val nhỏ

- Không chọn hyperparameters dựa trên OOD val từng fold (ESCA val min=1, TGCT val min=1)
- Grid search trên IND val (ổn định), giữ cố định cho tất cả folds
- Report mean ± std trên 10-fold cross-validation

---

## 8. Tóm tắt Đóng góp của Từng Bài báo Tích hợp

| Bài báo | Đóng góp cụ thể vào Framework |
|---|---|
| **PETAL** | Student-teacher probabilistic framework; $\mathcal{L}_{PETAL}$ với Bayesian regularizer; FIM-based restoration |
| **WeiContrast-DaPC** | Module B: DaPC pseudo-label correction; confidence-based domain-shift detection |
| **NOTE** | Module E: OOD score từ anchor-teacher confidence gap; adaptive learning rate |
| **CascadeTTA** | Inspiration cho $\mathcal{L}_{task}$ — đồng bộ feature extractor với classifier (TCP prompts) |
| **Frozen-to-Paraffin** | Module A: WSI bag augmentation bằng patch subsampling; $r_{patch} \in \{0.50, 0.75\}$, $K=8$ |
| **MergeSlide** | Base model $\tilde{\theta}_{1:T}$; TCP inference; `task_prompts.pt`; TITAN backbone; frozen MLP |

---

## 9. Rủi ro và Hạn chế

**R1 — SWAG posterior quality:** Nếu `train_swag.py` chạy quá ít epochs, $\sigma^2_p$ không chính xác → regularizer không phân biệt tốt tham số quan trọng. Mitigation: chạy ít nhất 5 epochs, kiểm tra variance distribution.

**R2 — OOD val quá nhỏ:** Không validate $\tau_{ap}$ và $\tau_{OOD}$ reliably trên fold nhỏ. Mitigation: dùng cross-task validation (tuning trên BRCA/RCC, áp dụng cho ESCA/TGCT).

**R3 — L_class và L_PETAL overlap:** Khi $\tau_c = 1$, $\mathcal{L}_{class} \approx \mathcal{L}_{PETAL}$ CE term → double counting. Mitigation: giữ $\tau_c = 0.07$ (khác biệt đủ lớn), hoặc ablation D-1 kiểm tra đóng góp thực sự.

**R4 — Adaptive LR không ổn định:** Sigmoid sharp khi OOD_score gần $\tau_{OOD}$ có thể tạo gradient oscillation. Mitigation: EMA smooth OOD score qua vài slides, hoặc dùng window average.

---

## 10. Workflow Thực tế

```bash
# Bước 1: Train (giữ nguyên)
bash scripts/finetune.sh

# Bước 2: Merge (giữ nguyên)
bash scripts/mergemodel.sh

# Bước 3: Compute SWAG posterior (MỚI — chạy một lần per config)
bash scripts/run_swag.sh                          # IND/forward
CONFIG=configs/default_ood.yaml \
MERGED_DIR=.../checkpoints_ood/merged \
SWAG_DIR=.../checkpoints_ood/swag_diagonal \
bash scripts/run_swag.sh                          # OOD

# Bước 4: TTA evaluation (MỚI)
bash scripts/run_tta_ind.sh                       # IND
bash scripts/run_tta_ood.sh                       # OOD

# Bước 5: Ablation studies
CONFIG=configs/default_abl_cotta.yaml bash scripts/run_tta_ind.sh
# ... (xem Section 6.6)
```

---

*Tài liệu cập nhật từ: PETAL (CVPR), WeiContrast-DaPC (IEEE TETC 2025), NOTE (NeurIPS), CascadeTTA (CIKM 2024), MergeSlide (arXiv 2025), Frozen-to-Paraffin.*
